const prompt = require('prompt-sync')()

let m1 = parseInt(prompt('enter marks in subject 1:'))
let m2 = parseInt(prompt('enter marks in subject 2:'))
let t = parseInt(prompt('enter total marks:'))

console.log(`% in subject 1: ${(m1/t)*100}`);
console.log(`% in subject 2: ${(m2/t)*100}`);